// This component has been removed to resolve build issues.
// The script generation feature is no longer supported in this application.
const ScriptGenerator = () => null;
export default ScriptGenerator;
